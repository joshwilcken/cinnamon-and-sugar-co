import React, { Component } from 'react'
import Header from '../Header/Header'

export default class Baking extends Component {
    render() {
        return (
            <div>
                <Header />
                Welcome to the Baking page!
            </div>
        )
    }
}
